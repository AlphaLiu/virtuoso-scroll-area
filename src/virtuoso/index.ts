/**
 * `virtuo-scroll-area/virtuoso` — virtualized list entry point.
 *
 * Requires the optional peer dependency `react-virtuoso`.
 */

export { VirtuosoScrollArea } from './virtuoso-scroll-area';
export type {
  VirtuosoScrollAreaHandle,
  VirtuosoScrollAreaProps,
} from './virtuoso-scroll-area';
