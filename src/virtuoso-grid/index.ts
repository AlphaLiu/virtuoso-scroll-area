/**
 * `virtuo-scroll-area/virtuoso-grid` — virtualized grid entry point.
 *
 * Requires the optional peer dependency `react-virtuoso`.
 */

export { VirtuosoGridScrollArea } from '../virtuoso/virtuoso-grid-scroll-area';
export type {
  VirtuosoGridScrollAreaHandle,
  VirtuosoGridScrollAreaProps,
} from '../virtuoso/virtuoso-grid-scroll-area';
